import BackgroundCanvas3 from './index.vue'
BackgroundCanvas3.name = 'BackgroundCanvas3'
// eslint-disable-next-line func-names
BackgroundCanvas3.install = function(Vue) {
  Vue.component(BackgroundCanvas3.name, BackgroundCanvas3)
}

export default BackgroundCanvas3